import { rejectWithdrawalSchema, withdrawalIdSchema } from "@/modules/withdrawals/withdrawal.schema"; import { rejectWithdrawal } from "@/modules/withdrawals/withdrawal.service"; import { requireRole } from "@/shared/auth/require-role"; import { errorResponse, successResponse } from "@/shared/http/api-response"; import { parseSchema } from "@/shared/validation/parse-schema";
interface Context { readonly params: Promise<{ readonly id: string }>; }
export async function POST(request: Request, context: Context): Promise<Response> { try { const user = await requireRole(["ADMIN", "COORDINATOR"]); const { reason } = parseSchema(rejectWithdrawalSchema, await request.json()); return successResponse({ withdrawal: await rejectWithdrawal(user.organizationId, user.id, parseSchema(withdrawalIdSchema, (await context.params).id), reason) }); } catch (error: unknown) { return errorResponse(error); } }
